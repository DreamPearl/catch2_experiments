# catch2_experiments

*Steps to generate the Makefile:*

`mkdir build`\
`cd build`\
`cmake ..`

*To compile we need to run make:*

`make`

After running the above commands, you will see the `final` executable file inside the build directory.

*To run final executable file:*

`./final`
